[project]
name = "transcriptor"
version = "0.1.0"
description = ""
authors = [
    {name = "Jonnieey", email = "johnjahi55@gmail.com"},
]
dependencies = [
    "audioread~=2.1",
]
requires-python = ">=3.9"
dynamic = ["classifiers"]
license = {text = "MIT"}

[project.urls]
homepage = ""

[build-system]
requires = ["pdm-pep517"]
build-backend = "pdm.pep517.api"

[tool]
[tool.pdm]
