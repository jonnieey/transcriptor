[project]
name = "transcriptor"
version = "0.1.0"
description = ""
authors = [
    {name = "Jonnieey", email = "johnjahi55@gmail.com"},
]
dependencies = [
    "audioread~=2.1",
    "python-magic~=0.4",
    "appdirs~=1.4",
    "readchar~=3.0",
    "questionary~=1.10",
    "click~=8.0",
    "tabulate~=0.8",
]
requires-python = ">=3.9"
dynamic = ["classifiers"]
license = {text = "MIT"}

[project.urls]
homepage = ""

[build-system]
requires = ["pdm-pep517"]
build-backend = "pdm.pep517.api"

[tool]
[tool.pdm]
[tool.pdm.dev-dependencies]
dev = [
    "pytest~=6.2",
]
